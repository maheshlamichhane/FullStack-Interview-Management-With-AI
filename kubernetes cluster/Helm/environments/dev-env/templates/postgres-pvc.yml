apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-mac-pvc
  namespace: dev
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mac-hostpath
  resources:
    requests:
      storage: 3Gi
