apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-mac-pvc
  namespace: prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mac-hostpath
  resources:
    requests:
      storage: 5Gi
